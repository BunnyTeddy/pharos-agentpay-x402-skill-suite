/**
 * Remotion config for the AgentPay presentation video.
 * All motion is driven in React components; no Tailwind or CSS animation plugin is used.
 */
import { Config } from "@remotion/cli/config";

Config.setVideoImageFormat("jpeg");
Config.setOverwriteOutput(true);
