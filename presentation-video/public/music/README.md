# Background music

`placeholder-ambient.wav` is an original generated placeholder pad for this presentation video.

To replace it later, drop a licensed music file into this folder and update the `staticFile()` path in `src/AgentPayPresentation.tsx`.
